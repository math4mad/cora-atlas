# Note: The Yoneda License — probe-ism as applied category theory
*(half-pager, for v3 §2; CT首读 = Bradley, What is Category Theory Anyway?, sha-pinned in chora/artifacts/external)*

**Thesis.** The garden's epistemics — never open the weights, aim fresh sentences, read the activations — is not heuristic humility but a theorem pattern: the Yoneda embedding sends an object X to the functor Hom(−, X); in locally small categories this embedding is fully faithful, i.e. **all morphisms into (or out of) X determine X up to unique isomorphism**. Translated: a concept space *is* its probe-response profile; "black-box only" methodology is licensed by category theory, provided the probe family is rich enough to approximate a representing set.

**Three import slots.**
1. *Probe design as hom-set sampling.* Our fingerprint rule (novel sentences, zero-hit audit) = choosing morphisms not factored through memorized composites. Open math question (frog work): how small can a probe family be while Yoneda-faithful up to our tolerance ε on centered cosines?
2. *Functorial transfer ⇒ P-B8.* Cross-domain competence should compose: if routing R sends probe contexts to spaces, and F is a meaning-preserving map between domains, accuracy should be ~invariant along the triangle. Operational test: chained metaphors (A→B→C vs direct A→C) at equal token budget; **decay ≪ 1 predicted if representations are functorial, decay ≈ 0.5 if transfer is lookup-tables.** (This is our falsifiable bridge to the compositionality wars.)
3. *Cross-family replication = natural isomorphism.* "The bulge is conserved in Llama" is strictly: the ΔW-angle diagrams of two base models are naturally isomorphic along a family of comparisons preserving all triangles — which is *why* we log angle tables, not raw weights.

**Honesty clause.** CT supplies grammar and new predictions (P-B8), not numbers; every degree above came from a T4, not a theorem. Birds name the wings; frogs count them.
